{
  "kind": "collectionType",
  "collectionName": "saved_views",
  "info": {
    "singularName": "saved-view",
    "pluralName": "saved-views",
    "displayName": "saved-view"
  },
  "options": {
    "draftAndPublish": false,
    "comment": ""
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "maxLength": 32
    },
    "slug": {
      "type": "string",
      "required": true
    },
    "roles": {
      "type": "json",
      "default": []
    },
    "visibility": {
      "type": "enumeration",
      "enum": ["private", "public", "roles"],
      "required": true,
      "default": "private"
    }
  }
}
